import React from 'react';

/**
 * Short dashed connector arrow between module cards in a lane track.
 */
export const FlowLink = (): React.JSX.Element => (
    <div className="fn-flow-link" aria-hidden="true" />
);
