# BMad

YANET2 tracks BMad Core and BMM 6.11.0 for Claude Code, Codex, and OpenCode. Start with the `bmad-help` skill.

`config.user.toml` and `_bmad-output` are intentionally ignored. Point `_bmad-output` at the team's access-controlled artifact store before working with shared planning artifacts.
