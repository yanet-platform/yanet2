[package]
name = "yanet-proxy-cp"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
libc = "0.2"
memory = { version = "0.1", path = "../../../common/rust/memory" }

[build-dependencies]
bindgen = { version = "0.72", features = ["experimental"] }
cc = "1.2"