[package]
name = "yanet-proxy-dp"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
libc = "0.2"
memory = { version = "0.1", path = "../../../common/rust/memory" }
bindings = { version = "0.1", path = "../bindings" }
state = { version = "0.1", path = "../state" }