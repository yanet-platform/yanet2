#include "controlplane.h"

int
decap_config_compile(struct decap_module_config *config) {
	(void)config;

	/*
		void *data = NULL;

		encode_uint32(v4_range_count);
		for () {
			encode_v4_addr(from);
			encode_v4_addr(to);
		}

		encode_uint32(v6_range_count);
		for () {
			encode_v6_addr(from);
			encode_v6_addr(to);
		}
	*/
	return 0;
}
