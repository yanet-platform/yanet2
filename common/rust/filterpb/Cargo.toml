[package]
name = "yanet-filterpb"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.85"

[dependencies]
netip = "0.3"
prost = "0.13"
serde = { version = "1", features = ["derive"] }
tonic = { version = "0.13", features = ["gzip"] }

[build-dependencies]
tonic-build = "0.13"
