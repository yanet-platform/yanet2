name: Go Lint

on:
  push:
    branches: [main]
    paths:
      - '**/*.go'
      - 'lint/style/**'
      - '.golangci.yml'
      - '.github/workflows/go-lint.yml'
      - '.github/actions/apt-packages/**'
  pull_request:
    paths:
      - '**/*.go'
      - 'lint/style/**'
      - '.golangci.yml'
      - '.github/workflows/go-lint.yml'
      - '.github/actions/apt-packages/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  stylelint:
    name: Custom stylelint linter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run tests
        run: go test ./lint/style/cmd/stylelint/

      - name: Run linter
        run: go run ./lint/style/cmd/stylelint/

  modernize:
    name: golangci-lint modernize
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: ./.github/actions/apt-packages
        with:
          packages: protobuf-compiler libpcap-dev

      - name: Install Go protobuf plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

      - name: Generate protobuf Go sources
        run: make proto-go

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2
          args: --timeout 5m
